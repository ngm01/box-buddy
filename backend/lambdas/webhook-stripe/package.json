{
  "name": "webhook-stripe",
  "private": true,
  "type": "module",
  "description": "POST /webhooks/stripe — grants credits after Stripe confirms payment",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^16.0.0"
  }
}
