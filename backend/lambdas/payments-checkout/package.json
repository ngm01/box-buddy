{
  "name": "payments-checkout",
  "private": true,
  "type": "module",
  "description": "POST /payments/checkout — creates a Stripe Checkout Session for a credit pack",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^16.0.0"
  }
}
